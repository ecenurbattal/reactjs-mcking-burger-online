import styled from 'styled-components';

export const Wrapper = styled.div`
    display: inline-flex;

    align-items: center;
`;

export const Value = styled.span`
    padding: 1em;
    color:white;
    font-size: 18px;
`;
